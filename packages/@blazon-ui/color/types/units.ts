export type RGB = [ColorType: 'rgb', Red: number, Green: number, Blue: number];

export type SRGB = [ColorType: 'srgb', Red: number, Green: number, Blue: number];

export type RelativeLuminance = number;

export type ContrastRatio = number;
